def Squareroot(n):
  x = n
  while True:
    root = (0.5)*(x+(n/x))
    if(abs(root-x)<0.1): # the error that can be allowed is 0.1
      break;
    x = root
  return root
print(Squareroot(36)) # ans: 6.00025298
